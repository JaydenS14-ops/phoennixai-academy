# Admin Access and Mandate Validation Notes

The public header now exposes an **Admin** link that routes to `/admin`. The route opens the dedicated Administrator Management Centre credential screen and does not expose any management data before authentication.

The live public mandate now renders the exact requested ending: “you’re kings, not servants - owners, you’re created to create.”

The floating back-to-top control appears after scrolling beyond the hero and returns the page to the top when invoked. Its control label is exposed as “Back to top” for assistive technologies.
