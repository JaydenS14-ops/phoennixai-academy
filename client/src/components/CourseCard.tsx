import { useEffect, useRef } from "react";
import { CheckCircle2 } from "lucide-react";
import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import PhoenixFlame from "@/components/PhoenixFlame";
import { useAcademyAnalytics } from "@/hooks/useAcademyAnalytics";
import { useTheme } from "@/contexts/ThemeContext";

export type CatalogCourse = { id: number; slug?: string; title: string; description: string; duration: string; pricePence: number; paymentLink: string | null; featured: number };
export const formatPrice = (pricePence: number) => new Intl.NumberFormat("en-GB", { style: "currency", currency: "GBP", maximumFractionDigits: 0 }).format(pricePence / 100);

export default function CourseCard({ course }: { course: CatalogCourse }) {
  const featured = Boolean(course.featured); const holidayFamily = course.slug === "holiday-family-bundle"; const badge = holidayFamily ? "Holiday family offer" : featured ? "Multi-course value" : null; const { track } = useAcademyAnalytics(); const { theme } = useTheme(); const standardDark = !featured && !holidayFamily && theme === "dark"; const cardRef = useRef<HTMLElement>(null); const viewedRef = useRef(false);
  useEffect(() => { const card = cardRef.current; if (!card || viewedRef.current) return; const observer = new IntersectionObserver(entries => { if (viewedRef.current || !entries.some(entry => entry.isIntersecting)) return; viewedRef.current = true; track("course_view", { pathway: course.title, detail: "Programme card visible" }); observer.disconnect(); }, { threshold: .55 }); observer.observe(card); return () => observer.disconnect(); }, [course.title, track]);
  return <article ref={cardRef} style={standardDark ? { backgroundColor: "#172231", color: "#F7F6F2" } : undefined} className={`academy-surface relative flex h-full flex-col overflow-hidden rounded-lg border p-6 transition-all duration-300 hover:-translate-y-1 hover:shadow-xl ${featured || holidayFamily ? "border-[#42B4E7]/45 bg-[#203641] text-[#F7F6F2] shadow-lg shadow-[#203641]/15" : "border-[#556970]/22 bg-white text-[#1F2426] shadow-sm"}`}><div className="absolute right-0 top-0 h-24 w-24 rounded-bl-full bg-[#42B4E7]/10" />{badge ? <div className="relative mb-6 flex w-fit items-center gap-1.5 rounded-full bg-[#42B4E7]/15 px-3 py-1 font-mono text-[9px] font-bold uppercase tracking-[0.1em] text-[#C5F0FF]"><PhoenixFlame className="h-3.5 w-3.5 text-[#42B4E7]" />{badge}</div> : null}<div className="relative mb-6"><p className={`font-mono text-[9px] uppercase tracking-[0.14em] ${featured || holidayFamily ? "text-[#A8E6FF]" : standardDark ? "text-[#AABFC5]" : "text-[#526269]"}`}>{course.duration}</p><h3 className="academy-title mt-3 font-display text-3xl font-bold leading-none tracking-wide">{course.title}</h3><p className={`academy-copy mt-4 font-body text-[15px] leading-6 ${featured || holidayFamily ? "text-[#F3F7F8]" : standardDark ? "text-[#D0DEE1]" : "text-[#425156]"}`}>{course.description}</p></div><div className="relative mt-auto">{holidayFamily ? <div><p className="font-display text-3xl font-bold">Holiday pricing</p><p className="mt-1.5 font-mono text-[10px] text-[#A8E6FF]">AVAILABLE FOR SCHOOL BREAKS</p></div> : <div className="flex items-end gap-2"><span className="font-display text-4xl font-bold">{formatPrice(course.pricePence)}</span><span className={`mb-1.5 font-mono text-[10px] ${featured ? "text-[#A8E6FF]" : standardDark ? "text-[#AABFC5]" : "text-[#526269]"}`}>PER MONTH</span></div>}<p className={`mt-3 font-body text-xs ${featured || holidayFamily ? "text-[#C5F0FF]" : standardDark ? "text-[#D0DEE1]" : "text-muted-foreground"}`}>{holidayFamily ? "For summer, bank-holiday, and half-term learning with children, siblings, and parents." : "Choose this route, then tell us whether the enquiry is for a learner, an adult, or an Agency opportunity."}</p><div className="mt-5 flex flex-wrap gap-2"><Button asChild className={featured || holidayFamily ? "rounded-md bg-[#ABA944] text-[#1F2426] hover:bg-[#C9C877]" : "rounded-md bg-[#556970] text-white hover:bg-[#3E4F55]"}><Link href="/apply" onClick={() => track("cta_click", { pathway: course.title, detail: holidayFamily ? "Holiday bundle enquiry" : "Choose pathway" })}>{holidayFamily ? "Ask about holiday sessions" : "Choose this pathway"}</Link></Button>{course.paymentLink && !holidayFamily ? <Button asChild variant="outline" className={featured ? "border-white/30 text-white hover:bg-white/10 hover:text-white" : standardDark ? "border-[#AABFC5]/50 text-[#E6F0F2] hover:bg-white/10" : "border-[#556970]/35 text-[#425156] hover:bg-[#EFF0EA]"}><a href={course.paymentLink} target="_blank" rel="noreferrer" onClick={() => track("cta_click", { pathway: course.title, detail: "Payment link" })}>Payment link <CheckCircle2 className="ml-1.5 h-4 w-4" /></a></Button> : null}</div></div></article>;
}
